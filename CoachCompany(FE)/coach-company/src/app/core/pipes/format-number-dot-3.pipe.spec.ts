import { FormatNumberDot3Pipe } from './format-number-dot-3.pipe';

describe('FormatNumberDot3Pipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNumberDot3Pipe();
    expect(pipe).toBeTruthy();
  });
});
